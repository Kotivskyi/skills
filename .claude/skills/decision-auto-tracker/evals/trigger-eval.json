[
  {"query": "ok final answer on the naming thing: the table is `orders`, the API resource is `/orders`, and the domain object is `Order` — no more `purchase` anywhere. update the glossary in docs/ and make sure this is written down somewhere we'll find it again", "should_trigger": true},
  {"query": "we went back and forth on this for an hour in the design review. decided: retries use exponential backoff capped at 5 attempts, then dead-letter. log this decision please", "should_trigger": true},
  {"query": "scratch what I said yesterday about Cloudflare for the DNS, we're staying on Route 53 because of the health-check integration. make sure the earlier decision gets marked as superseded", "should_trigger": true},
  {"query": "for v1 we skip subscriptions entirely, one-time purchases only. the billing team agreed this morning. remember we decided this", "should_trigger": true},
  {"query": "the debounce for the outside-fence detection is 500ms, that's final, the hardware lead measured it on the bench. Track it.", "should_trigger": true},
  {"query": "let's go with option B from your comparison (the shared substrate carved into its own delivery change). that's the architecture from now on", "should_trigger": true},
  {"query": "quick one: Dana owns the ingest service going forward, not the platform team. can you note that where we keep our decisions", "should_trigger": true},
  {"query": "we just agreed in standup that prices are stored as integer cents everywhere, never floats. every service. write this down", "should_trigger": true},
  {"query": "can you go through decisions/log/ in this repo and tell me which entries are actually noise — stuff about tickets or the agent's own workflow that shouldn't be in there", "should_trigger": true},
  {"query": "to clarify the 30s checkout timeout we decided last month: it's 30s of provider silence, a pending heartbeat restarts the clock. same decision, just precise. record the clarification", "should_trigger": true},
  {"query": "settled it with the firmware guys on the call: FPort 12 is the health frame and nothing else ever uses that port. can you get that recorded before we all forget", "should_trigger": true},
  {"query": "going forward every new service gets its own Postgres schema, not its own database instance. we're not paying for 12 instances. note it down", "should_trigger": true},
  {"query": "decided against the multi-region thing for launch — EU only until someone actually pays us from another continent. capture that with the reasoning, the infra bill was the argument", "should_trigger": true},
  {"query": "3 decisions from today's arch review, write them all down: (1) path versioning /v1/ not headers (2) rate limits per api key at 600rpm (3) webhook retries exponential backoff 5 attempts then DLQ", "should_trigger": true},
  {"query": "i want the session timeout to be 20 minutes idle, 12 hours absolute. that's the policy, applies to web and mobile both. please record it properly so the next person doesn't change it on a whim", "should_trigger": true},
  {"query": "yeah do it the second way — put the retry logic in the consumer not the producer. that's how we'll do all the queues from now on", "should_trigger": true},
  {"query": "reversing myself on the monorepo. we're splitting the mobile app into its own repo after all, the CI times got unbearable. the old decision needs to point at this one", "should_trigger": true},
  {"query": "for the record: we are NOT supporting IE11 or anything below Safari 15. marketing signed off. put it in the log with the date", "should_trigger": true},
  {"query": "call it a `Pasture` not a `Paddock` in all the code and the API. the spanish term `piquete` only shows up as a labelled translation, never as the canonical name. write it down before someone renames it back", "should_trigger": true},
  {"query": "after the incident we're setting the p99 latency budget for checkout at 800ms. anything above that blocks a release. this is a standing rule now, save it", "should_trigger": true},
  {"query": "we talked about it and decided the admin app stays server-rendered, no SPA. too much auth complexity for the payoff. can you log that decision with the reasoning", "should_trigger": true},
  {"query": "one more from the call — device configs are immutable, a change writes a new config id and bumps the version. never edit in place. that's the model now", "should_trigger": true},
  {"query": "we're standardising on UTC in the database and doing all timezone conversion at the presentation layer. no more local timestamps in columns. record this", "should_trigger": true},
  {"query": "the answer to the open question from last week: yes, chirpstack gets one instance per tenant, not a shared one. isolation beat the cost argument. log it and reference the earlier discussion", "should_trigger": true},
  {"query": "put this somewhere permanent: soak tests run for 19 hours minimum before a firmware release goes out. anything shorter doesn't count as a soak", "should_trigger": true},
  {"query": "final call on the auth thing — firebase custom tokens for mobile v0, we revisit when we do web. don't let me forget the reasoning was time to market", "should_trigger": true},
  {"query": "add a decision entry: the public API never exposes internal numeric ids, SKU is the external key everywhere. a partner integration already broke on this once", "should_trigger": true},
  {"query": "audit our decision log please — 400-odd files in decisions/log and i suspect half of them are one-off ticket stuff. classify them and tell me what percentage shouldn't be there", "should_trigger": true},

  {"query": "waive the local green run for TICKET-1066 and open the PR, the integration pin covers it", "should_trigger": false},
  {"query": "ship the counter fix as a standalone PR from main, don't wait for the big measurement change, and close the ticket with the gap noted", "should_trigger": false},
  {"query": "one AI review round per PR is enough — the agent reads the findings, fixes or rejects each, pushes once, never re-triggers the reviewer. put that in AGENTS.md", "should_trigger": false},
  {"query": "the stop-hook should fall back to WSL when there's no native C++ compiler instead of failing the turn. fix the hook script", "should_trigger": false},
  {"query": "hmm we could maybe put sessions in redis… or memcached, simpler to run. or just postgres with a TTL column. let me think about it over lunch", "should_trigger": false},
  {"query": "write an ADR in docs/decisions/ for adopting Kafka as the event backbone, full template with consequences and alternatives considered", "should_trigger": false},
  {"query": "rename `foo_handler` to `order_created_handler` across the codebase and be more concise in your commit messages from now on", "should_trigger": false},
  {"query": "base the hotfix branch on fresh main not on the release branch, and squash before you push", "should_trigger": false},
  {"query": "what did we decide about the multicast margin? grep the decisions folder and tell me, don't change anything", "should_trigger": false},
  {"query": "the MCP config for grafana should live in ~/.codex/config.toml and wrap the server with `op run` so the token comes from 1password. set that up for me", "should_trigger": false},
  {"query": "update the openspec change delta for the grazing-plans capability — the scenario about overlapping breaks needs to match the new validation we just wrote", "should_trigger": false},
  {"query": "i'm torn between tanstack query and swr for the admin app. can you lay out the tradeoffs given we already use tanstack table? not deciding today, just want the comparison", "should_trigger": false},
  {"query": "for this refactor only, put the helpers in utils/ and we'll move them later when the module lands. don't overthink it", "should_trigger": false},
  {"query": "read decisions/log/2026-08-01-01-postgres-for-order-store.md and summarise what we decided and why, i'm writing the onboarding doc", "should_trigger": false},
  {"query": "the CI e2e job times out at 10 minutes. bump it to 20 and add a retry-on-flake step to the workflow yaml", "should_trigger": false},
  {"query": "from now on always run the type checker before you tell me a task is done, and don't say 'done' until the output is clean", "should_trigger": false},
  {"query": "we need a proper RFC for the billing migration — stakeholders, phases, rollback plan, the whole thing. start a draft in docs/rfcs/", "should_trigger": false},
  {"query": "i changed my mind about the button colour, make it the darker blue from the palette. and tighten the padding a bit", "should_trigger": false},
  {"query": "skip the changelog entry for this one, it's an internal-only fix and nobody outside sees it", "should_trigger": false},
  {"query": "add a CLAUDE.md section telling agents to use pnpm not npm in this repo, and to never commit directly to main", "should_trigger": false},
  {"query": "which of these two approaches would you recommend for the cache invalidation — TTL or explicit busting? give me your reasoning, i'll decide after i talk to the team", "should_trigger": false},
  {"query": "for the demo tomorrow just hardcode the tenant id in the config, we'll wire up the real lookup next week", "should_trigger": false},
  {"query": "our decisions/log folder has 400 files and the frontmatter is inconsistent. write me a python script that parses them all into a csv so i can pivot it in sheets", "should_trigger": false},
  {"query": "the adversarial review prompt in our openspec schema is too aggressive, it blocks on speculative hardening. soften that prompt so it only flags real risks", "should_trigger": false},
  {"query": "go ahead and merge the PR, it's green and the tech lead already approved it this morning", "should_trigger": false},
  {"query": "explain why we went with cloud run instead of GKE — i think there's an ADR about it somewhere but i can't find it", "should_trigger": false},
  {"query": "set the retry count to 5 in the config file for the staging environment so i can reproduce the timeout bug", "should_trigger": false},
  {"query": "draft the release notes for v0.4 from the merged PRs since the last tag, group them by area", "should_trigger": false}
]
